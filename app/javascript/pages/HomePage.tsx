import styles from './HomePage.module.css'

export function HomePage() {
  return (
    <main className={styles.root}>
      <h1>ReservaYa</h1>
      <p>Frontend inicializado — TanStack Router + arquitectura por capas.</p>
    </main>
  )
}
